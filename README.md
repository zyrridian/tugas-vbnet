# VB .NET RentalMobil App (Windows Form App)
how to run the project:
- run the sql query (`database.sql`) to create a new database, tables, and generate dummy data.
- turn on local mysql.
- run the app.
